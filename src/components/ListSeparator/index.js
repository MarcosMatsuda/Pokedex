import React from 'react';
import { View } from 'react-native';

const ListSeparator = () => {
  return (
    <View
      style={{
        height: 1,
        width: '100%',
        backgroundColor: '#CED0CE',
      }}
    />
  );
};

export { ListSeparator };
